<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/CustomTitleLayout"
	android:layout_width="fill_parent" android:layout_height="fill_parent"
	xmlns:android="http://schemas.android.com/apk/res/android">

	<TextView android:layout_width="wrap_content"
		android:layout_height="wrap_content" android:id="@+id/appTitle"
		android:text="@string/app_name" />

	<TextView android:layout_width="wrap_content"
		android:layout_height="wrap_content" android:id="@+id/rightText"
		android:text="right" android:layout_alignParentRight="true" />

	<ProgressBar android:id="@+id/title_progress_bar"
		style="?android:attr/progressBarStyleSmall" android:layout_width="wrap_content"
		android:layout_height="wrap_content" android:layout_centerVertical="true"
		android:layout_toLeftOf="@+id/rightText" android:layout_alignTop="@+id/appTitle"
		android:paddingRight="3px" android:visibility="invisible" />

</RelativeLayout>